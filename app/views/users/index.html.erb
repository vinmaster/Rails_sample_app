<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
	<%= render @users %>
	<!-- refactored, same as above
	<% @users.each do |user| %>
		<%= render 'user', user: user %>
	<% end %>
	-->
</ul>

<%= will_paginate %>